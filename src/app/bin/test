#!/usr/bin/env bash

while [ "${1:-}" != "" ]; do
    case "$1" in
        "-f" | "--file")
        file="$2"
        shift
        ;;
        "-w" | "--watch")
        watch="--watch"
        ;;
    esac
    shift
done

cmd="TEST_SPEC=$file npx ava --progress ${watch}"

echo $cmd
eval $cmd
